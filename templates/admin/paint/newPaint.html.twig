{% extends 'base.html.twig' %}

{% block title %}Nouvelle Peinture{% endblock %}

{% block body %}

    <div class="">
        <div>
            <h1 class="display-5 m-0 text-center">Peinture</h1>
        </div>
        <a href="{{ path('app_paint') }}" title="Retour à la liste" class="btn btn-outline-secondary">
            Retour à la liste
        </a>
    </div>
    <div class="w-50 mx-auto">
        {{ form(form) }}
    </div>

{% endblock %}
